create a `.env file`  consisting of the following

```bash
NFT_META_DATA_URL="******"
```



RUN `npm install --force` beause of the legacy dependency

for development run `npm run dev`


for production run `npm run dev`

